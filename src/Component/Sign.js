import { useState } from "react";
import DvrIcon from "@mui/icons-material/Dvr";
import Main from "./Main";

export default function Sign() {
  // const [loginStatus, setLoginStatus] = useState(undefined);

  // if (loginStatus !== undefined) return <Main />;

  // return (
  //   <div className="container">
  //     <div className="login-box animat">
  //       <h1>Welcome for to do list</h1>
  //       <DvrIcon fontSize="large" />
  //       <div className="select-botton">
  //         <button onClick={() => setLoginStatus(true)}>
  //           Login by admin account
  //         </button>
  //         <button onClick={() => setLoginStatus(false)}>
  //           Login by user account
  //         </button>
  //       </div>
  //     </div>
  //   </div>
  // );

  return <Main />;
}
